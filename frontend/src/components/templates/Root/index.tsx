import React from "react";
import styled from "styled-components";

import Wrapper from "../../organisms/Wrapper";

type Props = {};

const RootTemplate: React.FC<Props> = ({}) => {
  return <Wrapper>Hello, Worlf</Wrapper>;
};

export default RootTemplate;
